AIOps 智能运维平台（Demo版）项目文档
1. 项目概述
1.1 痛点
运维团队每天面对海量日志和指标，人工排查效率低，故障发现滞后。我们希望构建一个轻量级 AIOps demo 系统，利用机器学习自动检测日志异常，辅助快速定位问题。

1.2 目标用户
运维工程师：快速查看异常日志，减少故障排查时间。

开发人员：通过日志分析优化代码。

1.3 使用场景
实时日志流接入，系统自动聚类并标记异常。

用户通过 Web 仪表盘查看异常趋势、详情，并可手动标注反馈。

1.4 核心功能
日志接入：支持上传文件或模拟流式日志。

异常检测：基于统计/ML 模型识别异常模式。

可视化仪表盘：展示异常数量、趋势、日志样本。

标注反馈：用户可纠正误报，用于模型迭代。

2. 需求与验收标准（PRD 摘要）
功能	验收标准
日志文件上传	支持 .log/.txt 格式，最大 10MB；上传后立即触发分析；上传成功/失败有明确提示。
实时日志模拟	点击“开始模拟”后，每秒生成一条随机日志，并实时推送至前端；停止模拟后不再推送。
异常检测算法	使用孤立森林算法（初始实现）；在测试数据集上召回率 >80%，误报率 <20%；结果在日志列表中标记为“异常/正常”。
仪表盘总览	显示今日异常总数、Top5 异常模式、异常趋势折线图（按分钟聚合）。
日志详情列表	表格展示时间戳、日志级别、内容、异常标签；支持按级别/异常筛选。
用户标注	每条日志右侧可点击“误报”/“确认异常”，记录反馈至数据库。
3. 技术栈与架构草案（arc.md 摘要）
3.1 项目边界
本地自用 demo，无需用户登录，不涉及支付/隐私数据。

性能：支持 100 条/秒日志处理；前端响应 <2s。

成本：尽量使用开源组件，部署于单机。

3.2 技术栈
层次	技术选型
前端	React + TypeScript + Ant Design + ECharts
后端	Node.js + Express + TypeScript
算法	Python + Flask（或直接 Node 调用 sklearn）
数据库	SQLite（本地存储日志和标注）
消息	WebSocket（实时推送）
部署	Docker Compose（整合前后端及算法服务）

**全局上下文：**

- **技术栈：** Vue 3 (Composition API), TypeScript, Pinia, Vue Router, Axios, Element Plus (UI库)。
- **代码风格：** 使用 `<script setup lang="ts">`，优先使用 Hooks 组合式函数，保持代码简洁。
- **命名规范：** 变量使用驼峰命名，组件使用大驼峰命名。

3.3 目录结构（草案）
text
/
├── frontend/          # React 前端
├── backend/           # Node.js 后端（API + WebSocket）
├── algorithm/         # Python 异常检测服务
├── data/              # SQLite 数据库文件
├── docker-compose.yml
├── project.md         # 本文件
├── rules.md           # 开发规范
└── reference/         # 标准实现示例
3.4 数据模型
Log 表：id, timestamp, level, content, is_anomaly (boolean), user_feedback (null/true/false)

AnomalyStats 表：聚合统计（可实时计算，不存储）

4. 当前进度（YYYY-MM-DD 更新）
已完成（MVP 切片）
项目初始化：创建 frontend/backend/algorithm 三个空服务，docker-compose 联通。

前端基础布局：导航栏 + 仪表盘空页面。

后端健康检查 API。

数据库连接与 Log 表迁移。

进行中
日志文件上传功能（前端表单 + 后端接收存储）。

孤立森林算法集成（Python 服务，接收日志文本返回异常概率）。

待办（按优先级）
P0 完成日志上传 → 调用算法 → 存储结果 → 前端展示日志列表的完整链路。

P0 实现 WebSocket 实时推送模拟日志。

P1 仪表盘异常趋势图表。

P1 用户标注功能（更新数据库并刷新列表）。

P2 性能优化和错误处理。

5. 已知问题与决策记录
问题：算法服务单独用 Python，增加了部署复杂度，但便于后续更换模型。决策：保留 Python 服务，通过 HTTP 与 Node 通信。

问题：SQLite 并发写性能可能不足。决策：demo 阶段可接受，如遇瓶颈再切换 PostgreSQL。

问题：前端模拟日志生成未限流，可能压垮后端。决策：前端每秒最多发 1 条，后端异步处理。

6. 一致性检查
每次大改动后需同步更新 prd.md（需求）、arc.md（架构）、project.md（进度）。

当前 arc.md 是否与代码结构一致？

当前待办是否反映最新需求优先级？