# ⚠️ 此文件由 k8s/scripts/seal-secrets.sh 生成，请勿手工修改！
#
# 当前为空模板（encryptedData 为空）。部署前置条件：
#   1. 集群已安装 sealed-secrets controller
#   2. 在可访问集群的机器上运行 ./scripts/seal-secrets.sh
#      该脚本会用 kubeseal 以集群公钥加密 k8s/secrets-plain/secret.yaml，
#      并覆盖本文件生成真实的 SealedSecret 数据。
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: aiops-secrets
  namespace: aiops
spec:
  encryptedData: {}
  template:
    metadata:
      name: aiops-secrets
      namespace: aiops
    type: Opaque
