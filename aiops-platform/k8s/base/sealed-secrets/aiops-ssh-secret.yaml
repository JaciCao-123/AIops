# ⚠️ 此文件由 k8s/scripts/seal-secrets.sh 生成，请勿手工修改！
#
# 当前为空模板。部署前置条件：
#   1. 集群已安装 sealed-secrets controller
#   2. 先在跳板机运行 k8s/scripts/rotate-ssh-key.sh 生成新私钥
#      （输出到 gitignored 的 k8s/secrets-plain/ssh-secret.yaml）
#   3. 在可访问集群的机器上运行 ./scripts/seal-secrets.sh
#
# 说明：旧私钥已泄露到 Git 历史，轮换后本 Secret 将只包含新私钥。
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: aiops-ssh-secret
  namespace: aiops
spec:
  encryptedData: {}
  template:
    metadata:
      name: aiops-ssh-secret
      namespace: aiops
    type: Opaque
